# Brief — <project>

*The always-loaded briefing every agent reads first: the story of what we're building and why. One
screen, stable, PII-free.*

## Story

<A short narrative a stranger understands: what this project is and the change it creates. If it has a
second identity (a rewrite, a fork), say so in a clause.>

## Why it exists

<The reason it's worth building: the problem, who feels it, and why the existing options fall short. When
a decision is unclear, agents come back here to resolve it. (The rules for *how* to work are in
`AGENTS.md`, not here.)>

## Users / ICP

- <who uses it, what they're trying to accomplish, and the qualities that matter most to them>

## Scope

- **Active areas:** <the product areas, features, or surfaces this project spans>
- **Out of scope:** <the tempting scope it deliberately refuses, so the focus stays sharp>

## How it fits together

<The main path through the product, in order — one line per step, each naming the area or contract that
owns it. The shape of the system, not its rules.>

- **<Thing>** — <what happens at this step, in one line>.
- **<Next thing>** — <what happens next>.

## External Systems

- `<system>` — <what the project relies on it for>

---
*Editing this file? Follow the standard first: [`guides/docs-brief.md`](./guides/docs-brief.md).*
